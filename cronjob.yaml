apiVersion: batch/v1
kind: CronJob
metadata:
  name: k8s-cost-optimizer
spec:
  schedule: "*/2 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: optimizer
            image: hritik18/k8s-optimizer:latest
            imagePullPolicy: IfNotPresent
          restartPolicy: OnFailure